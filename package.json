{
  "name": "posthog-growth-engineer-skill",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run syntax",
    "typecheck": "npm run syntax",
    "syntax": "for f in scripts/*.mjs tests/*.mjs; do node --check \"$f\"; done",
    "test": "node --test tests/*.test.mjs",
    "quality": "npm run typecheck && npm run build && npm test"
  }
}
